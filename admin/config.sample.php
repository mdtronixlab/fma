<?php
/**
 * Copy this file to config.php (same folder) and set your own password.
 * config.php is gitignored on purpose — never commit real credentials.
 *
 * Just edit the plain password below — no hashing step needed. Login
 * still checks it with a timing-safe comparison (see admin/index.php).
 */

return [
    'password' => 'REPLACE-WITH-A-REAL-PASSWORD',
];
